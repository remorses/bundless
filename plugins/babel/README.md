## Example

```ts
// bundless.config.js
const { BabelPlugin } = require('@bundless/plugin-babel')

module.exports = {
    plugins: [BabelPlugin({
        babelOptions: {
            plugins:[
                
            ]
        }
    })],
}
```
